<h1><%= t('.title') %></h1>

<h3><%= t('.slogan') %></h3>

<nav>
  <% @projects.each do |project| %>
    <%= link_to project.title, project_path(project) %>
  <% end %>
</nav>

<nav>
  <%= link_to t('.area_owner'), new_project_owner_session_path unless project_owner_signed_in? %>
</nav>